{
  "_comment": "Default values for SharedPreferences entries that the IME and SettingsActivity read. These are seeded on first launch (only for keys the user has not already set). Users can copy this file from device storage, edit it, and re-import via Settings → Import.",
  "prefs": {
    "lang": "GENERAL",
    "dark": true,
    "amoled": false,
    "haptic": true,
    "key_sound": false,
    "key_sound_volume": 50,
    "key_height_scale": 1.0,
    "show_pc_keys": false,
    "theme_kind": "preset",
    "bg_color":     "#1A1A2E",
    "key_color":    "#252545",
    "text_color":   "#E8E8FF",
    "accent_color": "#00E5FF"
  }
}
